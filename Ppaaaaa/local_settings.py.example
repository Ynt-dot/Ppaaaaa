# Настройки для конкретного окружения. Скопируйте этот файл в local_settings.py
# и отредактируйте под свои нужды.

import os
from .settings import BASE_DIR

DEBUG = True  # На сервере поставьте False

ALLOWED_HOSTS = ['localhost', '127.0.0.1', 'ваш-домен.ru']

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',  # или 'mysql', 'postgresql'
        'NAME': 'db.sqlite3',  # для MySQL укажите имя базы
        # Для MySQL добавьте USER, PASSWORD, HOST, PORT
    }
}

SECRET_KEY = 'your-secret-key-here'

STATIC_ROOT = os.path.join(BASE_DIR, 'static')
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# Настройки почты для продакшена
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.reg.ru'  # или mail.ppaaaaa.ru, уточните
EMAIL_PORT = 465
EMAIL_USE_SSL = True  # если порт 465, если порт 587, используйте EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'noreply@ppaaaaa.ru'
EMAIL_HOST_PASSWORD = 'ваш_пароль'
DEFAULT_FROM_EMAIL = 'noreply@ppaaaaa.ru'

# Для ссылок в письмах (ваш домен уже https, если настроили SSL)
SITE_URL = 'https://ppaaaaa.ru'
